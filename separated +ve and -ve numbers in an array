//---------- Using C


    #include<stdio.h>
    #include<stdlib.h>
    int main(){
        int n,i,j;
        scanf("%d",&n);
        int *arr=(int *)malloc(n*sizeof(int));
        for(i=0;i<n;i++)scanf("%d",&arr[i]);
        for(i=0,j=n-1;i<j;j--){
            if(arr[j]<0){
                while(1){
                    if(arr[i]<0)i++;
                    else break;
                }
                int temp=arr[i];
                arr[i]=arr[j];
                arr[j]=temp;
            }
        }
        for(i=0;i<n;i++)printf("%d ",arr[i]);
    }
